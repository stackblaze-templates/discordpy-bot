services:
  - type: web
    name: discordpy-bot
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    autoDeploy: false
    envVars:
      - key: DISCORD_TOKEN
        sync: false
